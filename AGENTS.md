READ ~/Projects/agent-scripts/AGENTS.MD BEFORE ANYTHING (skip if missing).

## Project Notes (Midlight)

- Package manager: `npm` (uses `package-lock.json`).
- Node: `>=22` required (Compact compiler tooling).
- Keep `npm run lint`, `npm test`, `npm run build` green.
- No audio scope unless explicitly requested.
